/** Texturas fotográficas — camadas de parede / papel (assets locais). */
import concreteWall from "../../assets/textures/concrete-wall.jpg";
import paperFiber from "../../assets/textures/paper-fiber.jpg";

export const textureAssets = {
  concreteWall,
  paperFiber,
} as const;
